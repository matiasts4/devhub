/**
 * Default OpenCode agent profile for interactive launches (Zed, workspace
 * terminals, bridge, swarm). Matches `agent.gentle-orchestrator` in
 * ~/.config/opencode/opencode.json (UI label: Gentle-Orchestrator).
 */
export const DEFAULT_OPENCODE_AGENT = 'gentle-orchestrator';
