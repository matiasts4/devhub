# terminal-workspace-window-mount

Workspace terminal windows (V1/V2/V3) keep all panel TTY surfaces mounted within the active workspace; visibility and pointer-events follow the active window, matching workspace tab behavior. Pizarra view switches update the active window without remounting dock PTY surfaces.

See `openspec/changes/terminal-window-mount-parity/` for the implementing change.