# DevHub — SDD Configuration
# Auto-generated by sdd-init on 2026-04-03
schema: spec-driven

context: |
  Tech stack: Next.js 16 (Turbopack), React 19, JS/JSX, Tailwind CSS 4, shadcn/ui, Radix UI
  Backend: Tauri 2 (Rust), better-sqlite3, node-pty, WebSocket (ws), Supabase auth
  Architecture: Pages Router + Tauri desktop shell, sidecar-backend (Express), devhub-mcp (Node ESM)
  Testing: Playwright E2E, Jest (devhub-mcp), next test (root), unit + integration layers
  Style: ESLint 9 (flat config), Prettier, Husky + lint-staged pre-commit hooks

strict_tdd: true

testing:
  test_runner:
    framework: next test (Jest-based) / Jest (devhub-mcp)
    command: npm test
  layers:
    unit:
      available: true
      tool: next test / Jest (devhub-mcp)
    integration:
      available: true
      tool: Jest (devhub-mcp) + custom integration tests
    e2e:
      available: true
      tool: playwright test
  coverage:
    available: true
    command: cd devhub-mcp && npm run test:coverage
  quality_tools:
    linter:
      available: true
      command: eslint src --ext .js,.jsx,.ts,.tsx
    type_checker:
      available: false
      command: jsconfig.json (no tsc — JS project)
    formatter:
      available: true
      command: prettier --write

rules:
  proposal:
    - Include rollback plan for risky changes
    - Identify affected modules/packages
  specs:
    - Use Given/When/Then format for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group tasks by phase (infrastructure, implementation, testing)
    - Use hierarchical numbering (1.1, 1.2, etc.)
    - Keep tasks small enough to complete in one session
  apply:
    - Follow existing code patterns and conventions
    - Load relevant coding skills for the project stack
  verify:
    - Run tests if test infrastructure exists
    - Compare implementation against every spec scenario
  archive:
    - Warn before merging destructive deltas (large removals)
