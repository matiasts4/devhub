{
  "name": "gc_stale_indexes",
  "description": "Remove stale index databases for repos whose directories no longer exist on disk.",
  "inputSchema": {
    "additionalProperties": false,
    "properties": {},
    "type": "object"
  }
}
