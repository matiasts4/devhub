# Project-scoped MCP config for DevHub workspace (higher priority than ~/.grok/config.toml)
# Mirrors .opencode/opencode.json — DevHub MCP loads .env.local from repo root (Supabase driver).

[mcp_servers.devhub]
command = "/home/matias/.nvm/versions/node/v24.14.0/bin/node"
args = ["/home/matias/ArxonLabs/devhub/devhub-mcp/server.js"]
enabled = true
# No env overrides: server.js loads ../.env.local (DEVHUB_AUTH_PROVIDER=supabase, etc.)