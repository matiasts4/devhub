{
  "name": "devhub-cli",
  "version": "0.1.0",
  "description": "CLI for DevHub — agent swarm orchestration and operations",
  "type": "commonjs",
  "bin": {
    "devhub": "./bin/devhub"
  },
  "scripts": {
    "test": "jest",
    "test:integration": "jest tests/integration/*.test.js tests/fixtures/seed-factory.test.js --runInBand --verbose",
    "test:all": "npm test && npm run test:integration"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
